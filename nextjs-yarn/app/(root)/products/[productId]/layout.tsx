import React from 'react'

const ProductIdLayout = ({children}: Readonly<{children: React.ReactNode}> ) => {
  return (
    <div>Ini dari Layout Product Id {children}</div>
  )
}
export default ProductIdLayout
